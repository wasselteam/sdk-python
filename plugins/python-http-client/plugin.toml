id = "python-http-client"
name = "Python Plugin using HTTP-client"
version = "0.0.0"
endpoint = "/python/http-client/"
component = "index.wasm"

[build]
cmd = "componentize-py -w http-plugin componentize app"
